# uploads 디렉토리 접근 허용
Options +Indexes
Order allow,deny
Allow from all

# 이미지 파일 직접 접근 허용
<FilesMatch "\.(jpg|jpeg|png|gif|webp|JPG|JPEG|PNG|GIF|WEBP)$">
    Order allow,deny
    Allow from all
</FilesMatch>

# PHP 실행 방지 (보안)
<FilesMatch "\.php$">
    Order deny,allow
    Deny from all
</FilesMatch>